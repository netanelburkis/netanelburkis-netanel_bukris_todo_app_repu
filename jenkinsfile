pipline {
    agent any
    stages {
        stage('Build docker image') {
            steps {
                echo 'Building...'
                sh '''
                    docker build -t app_project /app
                '''
            }
        }
    }
}
